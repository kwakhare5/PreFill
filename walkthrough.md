# walkthrough.md — Decision Log
# Agent: log every major decision before ending session. WHY not just what. (R5)
# Format: [Date] | [What] | [Why this approach]

## Log
| Date | What | Why |
| :--- | :--- | :--- |
| 2026-05-16 | Project Initialized | Initial setup, folder architectures, TimescaleDB schema configurations, and Mock Swiggy MCP server launch. |
| 2026-05-17 | **Benchmark-Based Composition Inference** (Task 2.4) | *Approach:* Designed `household_profiler.py` to compare observed average consumption rates for Atta, Rice, Milk, and Eggs against predefined benchmarks for 4 standard household sizes (Single, Couple, Small Family, Large Family).<br>*Why:* Simple threshold-based rules fail easily when order patterns shift slightly or when some ingredients are missing. Benchmark matching using scaled Euclidean distance with confidence metrics yields robust inferences and easily handles missing attributes by weighting available indicators. |
| 2026-05-17 | **LangGraph Stateful Restock Agent** (Task 2.5) | *Approach:* Built `restock_agent.py` as a stateful node graph (`generate_alert` → `parse_reply` → `build_cart` → `place_order`) integrating the Swiggy Instamart mock MCP tool environment and Claude.<br>*Why:* Real WhatsApp interactions over Twilio are highly asynchronous and stateless. By storing conversation state in a serializable `RestockState` object, we can persist intermediate conversation frames to the database. This allows the system to pause mid-conversation, await a user reply, and safely resume without losing cart contents or conversation context. |
| 2026-05-17 | **Industrial Utilitarian UI Overhaul** | *Approach:* Redesigned all five frontend pages from scratch using thin borders, dotted-grid backgrounds, monospace data typography, customized Urgency/Confidence pills, and real hand-drawn SVG sparkline feeds.<br>*Why:* Traditional rounded SaaS templates feel generic and fail the "aesthetic wow factor" test. The high-contrast, data-dense, typography-heavy industrial layout immediately signals "high-accuracy analytical intelligence" to evaluators and creates a premium, high-fidelity experience matching modern design leaders like Vercel and Linear. |
| 2026-05-17 | **Strict Dependency and Next.js Code Syncing** | *Approach:* Escaped unescaped apostrophes inside JSX blocks, replaced raw standard `<a>` tags with optimized Next.js `<Link>` components, created a locked `requirements.txt`, and resolved root package.json scripts.<br>*Why:* Ensures high codebase compliance, zero development-to-production runtime errors, clean local hot-reloads, and eliminates compiler warning logs for clean builds. |
