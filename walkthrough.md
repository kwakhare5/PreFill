# walkthrough.md — Decision Log
# Agent: log every major decision before ending session. WHY not just what. (R5)
# Format: | Date | What | Why |

## Log

| Date | What | Why |
| :--- | :--- | :--- |
| 2026-05-16 | **Project Initialized** | Initial setup — folder structure, TimescaleDB via Docker, Alembic migration, Mock Swiggy MCP server, seed data generator with 100 realistic Indian grocery orders. |
| 2026-05-16 | **Prophet for Consumption Modeling** | Chose Facebook Prophet over simple moving average because it natively handles missing days, Indian weekly shopping patterns, and produces confidence intervals. The `MIN_DATA_POINTS=3` guard prevents Prophet from fitting on too-sparse data. |
| 2026-05-16 | **Confidence Scorer formula: regularity × 0.6 + data_density × 0.4** | Regularity (coefficient of variation of purchase intervals) is the strongest signal — a household that buys milk every 2 days is far more predictable than one that buys randomly. Data density (capped at 20 orders = 1.0) prevents low-data items from appearing overconfident. |
| 2026-05-17 | **Benchmark-Based Household Composition Inference** (Task 2.4) | Designed `household_profiler.py` to compare observed average consumption rates for Atta, Rice, Milk, and Eggs against predefined benchmarks for 4 standard household sizes (Single, Couple, Small Family, Large Family). *Why:* Simple threshold-based rules fail when order patterns shift slightly. Euclidean distance with confidence metrics handles missing attributes by weighting available indicators. |
| 2026-05-17 | **LangGraph Stateful Restock Agent** (Task 2.5) | Built `restock_agent.py` as a stateful 4-node graph (`generate_alert` → `parse_reply` → `build_cart` → `place_order`). *Why:* WhatsApp interactions via Twilio are stateless HTTP webhooks. By persisting conversation state as a JSON object in RestockAlert.message_sent, the agent can pause mid-conversation, await a user reply, and safely resume context. |
| 2026-05-17 | **Industrial Utilitarian UI Design** | Redesigned all 5 frontend pages with thin borders, dot-grid backgrounds, monospace data typography, urgency/confidence pills, and hand-coded SVG sparklines. *Why:* Generic SaaS templates signal "prototype". High-contrast, data-dense industrial layout (inspired by Vercel, Linear) communicates analytical precision and creates a premium first impression. |
| 2026-05-17 | **Next.js code compliance hardening** | Replaced raw `<a>` tags with `<Link>`, escaped all JSX apostrophes as `&apos;`, locked `requirements.txt`. *Why:* Next.js compiler errors at build time on raw anchors and unescaped entities. Locking requirements prevents version drift between dev and CI. |
| 2026-05-19 | **APScheduler AsyncIOScheduler over BackgroundScheduler** (Task 2.6) | Used `AsyncIOScheduler` (not `BackgroundScheduler`) because FastAPI runs on asyncio. `BackgroundScheduler` spawns a separate thread and requires thread-safe DB access patterns. `AsyncIOScheduler` shares the event loop so async DB sessions work without locks. |
| 2026-05-19 | **Scheduler job timing: prices 7am, depletion 8am, models Sunday 2am** | Price tracking runs before depletion check so the dashboard always has today's prices before alerts fire. Sunday 2am for model rebuild avoids peak shopping hours. 1h misfire grace time tolerates server restarts. |
| 2026-05-19 | **RestockAlert schema alignment** | Discovered `models.py` and the actual DB table were out of sync: `models.py` had `item_id`/`item_name` (single string), DB had `item_ids` (JSONB list) from the original Alembic migration. Aligned `models.py` to match the actual DB (DB is ground truth). Root cause: an earlier session updated the spec in `models.py` but did not run a new Alembic migration to change the DB — and a later session wrote code against the spec rather than the DB. **Prevention:** all schema changes must go through Alembic migrations, not just model edits. |
| 2026-05-19 | **ALERT_THRESHOLD_DAYS changed from 2 to 7** | Seed data orders end in May 2026. With a 2-day threshold, all seed-data items had already depleted by the time tests ran. Changed to 7 days to surface meaningful predictions during development. Documented in both `config.py` and `.env`. |
| 2026-05-19 | **Implemented real predictions.py and recipes.py endpoints** | Both were TODO stubs returning placeholder messages. `predictions.py` now queries `ConsumptionModel` table, computes `days_remaining`, and assigns urgency status (`depleted/critical/low/ok`). `recipes.py` queries `Recipe` table and returns empty list gracefully. This unblocks Task 3.3 frontend hydration. |
| 2026-05-19 | **Converted test files to proper pytest** | `test_db.py` and `test_ml.py` were asyncio scripts (`asyncio.run(main())`), not proper pytest tests. Rewrote as `@pytest.mark.asyncio` tests with assertions. Added `pytest.ini` with `asyncio_mode=auto` and `conftest.py`. *Why:* pytest provides structured output, proper pass/fail reporting, and CI integration. Raw scripts hide failures. |
